{
  "name": "exatec-regreso-a-casa",
  "version": "1.0.0",
  "private": true,
  "description": "Link-in-bio landing page for the EXATEC \"Regreso a Casa\" alumni reunion (Tec de Monterrey, Campus Monterrey). Static site for Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
